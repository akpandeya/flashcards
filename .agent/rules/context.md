---
trigger: always_on
---

I have a flashcard web app hosted on GitHub Pages (HTML/CSS/JS). I want to add new features while keeping the app 'Local-First' (privacy-focused, no backend database). All user progress must be saved in the browser's LocalStorage. I want the design to be colorful and playful.